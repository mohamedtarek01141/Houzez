export type property = { id: number, name: string, location: string, price: number, ProductType: string };
export type properties = property[];

interface IProperty {
  id: number;
  name: string;
  location: string;
  price: number;
  ProductType: string;
}
